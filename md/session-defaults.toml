schema_version = 1

# These settings describe a replaceable session occupying the MD seat.
# They are not part of the seat's durable identity.
runner = "codex-cli"
model = "gpt-5.6-sol"
reasoning_effort = "high"
sandbox_mode = "workspace-write"
approval_policy = "on-request"
approvals_reviewer = "auto_review"
