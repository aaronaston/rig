# Operator direction: Rig core and test instance

Date: 2026-09-09. Source: [verbatim operator direction](../../raw/2026-09-09-core-instance-github-direction.md).

Aaron accepted a private `aaronaston/rig` repository. He chose a worker-free
`main` core and a `test-instance` branch for the current Nadia environment,
with regular merges from `main` into that instance. The decision distinguishes
the instance integration branch from the worker's own isolated implementation
branch.
